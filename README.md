# nesemu [WIP]
NES emulator in c++

## Dependencies:

sdl2, yaml-cpp, conan package manger

## To Start on debian:
`$ ./init.sh`

## on other systems
install conan, download & compile & install sdl2 and yaml-cpp